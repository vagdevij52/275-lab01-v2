syntax = "proto3";

option java_package = "com.message.proto";

service PollServerProcessor{
	rpc poll(PollServerRequest) returns (PollServerReply);
}


message PollServerRequest{
	int32 client_id = 1;
}

message PollServerReply{
	string filename = 1;
}

message Empty{
}